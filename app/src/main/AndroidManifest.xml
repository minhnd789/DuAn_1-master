<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.cao.nang.duan">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".activity_binh_luan"></activity>
        <activity android:name=".activity_nhom_chat" />
        <activity android:name=".activity_login" />
        <activity android:name=".activity_tim_benh_vien" />
        <activity android:name=".ListListDrug" />
        <activity android:name=".ListFindHospital" />
        <activity android:name=".ListSick" />
        <activity android:name=".ListDrug" />
        <activity
            android:name=".ListCategoryDrug"
            android:parentActivityName=".ListCategoryDrug" />
        <activity android:name=".WelcomeScreen" />
        <activity
            android:name=".MainActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>